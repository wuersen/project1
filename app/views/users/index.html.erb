<h1>All Users</h1>
<% @users.each do |user| %>
  <p>
    Name <%= link_to user.name, user %>
  </p>

    <% user.photos.each do |photo| %>
    <%= link_to image_tag(photo.image, :class => 'thumb'), photo %>
    <% end %>
<% end %>
